
# MovieRecommendationWebApp
# MovieRecommendationWebApp Instructions to Run Locally
Backend Setup
  # Open a terminal
  cd backend
  
  # Create virtual environment
  python -m venv venv
  source venv/bin/activate  # (Windows: venv\Scripts\activate)
  
  # Install dependencies
  pip install -r requirements.txt
  
  # Set your OpenAI key
  echo "OPENAI_API_KEY=your_api_key_here" > .env
  
  # Run FastAPI
  uvicorn main:app --reload
Frontend Setup
  # In a separate terminal
  cd frontend
  npm install
  npm start
Frontend runs at 👉 http://localhost:3000
Open http://localhost:3000
Enter your movie preferences (e.g., “Action movies with strong female leads”)
Click Get Recommendations
The app displays 3–5 recommended movies from the OpenAI API.